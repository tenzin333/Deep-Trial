# moves the project source files to a new directory called app, and 
# creates an __init__.py file in the app directory to make it a package. 
# The __init__.py file is used to initialize the package and can be
# left empty or contain code that needs to be executed when the package is imported.